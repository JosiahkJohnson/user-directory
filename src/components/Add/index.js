//imports
import React from "react";

//out Add function component, will contain a form and submit button to add another user to the directory
function Add(){
    console.log("Add Page loaded");
    return(
        <div>
            <h1>This component will contain a form soon</h1>
            <p>This form will add more users to the directory</p>
            <button>With this submit button</button>
        </div>
    );
}

export default Add;